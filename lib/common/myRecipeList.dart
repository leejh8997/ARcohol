import 'package:flutter/material.dart';

class MyRecipeListPage extends StatelessWidget {
  const MyRecipeListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('목록 페이지', style: TextStyle(color: Colors.white)),
    );
  }
}